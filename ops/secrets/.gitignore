# Never commit a decrypted env to this folder.
# SOPS-encrypted files (.enc.env / .enc.yml / .enc.json) are
# explicitly safe because they're encrypted at rest.

*.env
!*.enc.env
*.yml
!*.enc.yml
*.json
!*.enc.json
*.plain
keys.txt
*.key
