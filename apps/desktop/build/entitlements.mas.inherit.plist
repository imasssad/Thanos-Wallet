<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  MAS child/helper-process entitlements. Electron spawns helper processes
  (GPU, renderer, utility); under the sandbox each must inherit the parent's
  sandbox rather than declare its own. This is the standard Electron MAS
  inherit file — keep it minimal.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.app-sandbox</key>
  <true/>
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
