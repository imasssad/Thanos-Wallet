# The Xcode project is regenerated by `safari:convert` (xcrun
# safari-web-extension-converter --force) — it's a build artifact, not
# source. Team / signing config that must persist belongs in a checked-in
# .xcconfig, not in the generated project.
xcode/
