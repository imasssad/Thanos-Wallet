# Safari Web Extension wrapper

Use the generated Chromium bundle and convert it with `safari-web-extension-converter`.

Set the following in Xcode before shipping:
- App Group identifiers
- iCloud / Keychain entitlements if used
- Team ID and provisioning profiles
- notarization and distribution signing
