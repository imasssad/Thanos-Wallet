{
  "comment": "Self-contained — apps/mobile is NOT a workspace member and is built in isolation on EAS (only this folder is uploaded), so it must NOT extend the monorepo-root tsconfig.base.json. Inlines the same compilerOptions (minus the @thanos/* workspace paths, which mobile never imports).",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "jsx": "react-jsx",
    "noEmit": true
  },
  "include": ["src/**/*", "App.tsx"]
}
