# NEVER commit store credentials. This directory holds the App Store
# Connect API key and the Google Play service-account key that
# eas.json's submit profiles point at. Ignore everything here except
# this rule + the README, so the folder exists in the repo but its
# contents never do.
*
!.gitignore
!README.md
