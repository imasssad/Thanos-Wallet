# Thanos Wallet — Audit Candidate Package (2026-08-27)

Prepared in response to the client's audit-readiness request (2026-08-27).
Everything in this package is either a real generated artifact (SBOM,
vulnerability scan, test results — pulled from an actual CI run) or a
pointer to existing, verified repo documentation. Nothing here is
fabricated or assumed; where something couldn't be produced or verified,
it's called out explicitly under "Known gaps" below rather than glossed
over.

## 1. Repository, branch, and immutable commit

- **Repository:** https://github.com/imasssad/Thanos-Wallet
- **Branch:** `main`
- **Frozen commit SHA:** `d10b031f08d7e4e5a961c858b1024b07ed84ae60`
- **Tag:** `audit-candidate-2026-08-27` (annotated, pushed to origin — `git checkout audit-candidate-2026-08-27` reproduces this exact tree)

## 2. Deployed build mapping — ⚠️ needs verification, not yet confirmed

I don't have SSH/VPS access, so I can't independently confirm what commit
is actually live on thanos.fi right now. **Action needed:** run this on
the VPS and report back:

```bash
cd /var/www/thanos-wallet && git rev-parse HEAD
```

If it doesn't match `d10b031f08d7e4e5a961c858b1024b07ed84ae60`, either
deploy the frozen candidate before the audit starts, or note the actual
deployed commit as the real target (auditors should test against what's
*live*, not just what's in git).

## 3. Build artifacts — ⚠️ partial

| Artifact | Status | Detail |
|---|---|---|
| Android APK | Available, but NOT from this exact commit | Latest: versionCode 22, built from commit `40a2c20`-ish lineage (before this Lithosphere/Solana-icon commit). `https://expo.dev/artifacts/eas/s2d3LAugldNYxEgkJWbPQYWWpV9QG0iXITjVnK6cXZ0.apk` |
| Android AAB | Same as above | `https://expo.dev/artifacts/eas/0NGmbJe94vHEoOTbEitSD25Jj7gcgDNW_6xMf1sLamI.aab` |
| iOS build | Available, submitted to App Store Connect, NOT from this exact commit | buildNumber 25, App Store Connect submission `b9166676-1e44-4c4a-b5e1-f4084ddff420` |
| Browser extension `.zip` | **Broken build — see Known gaps #1** | Most recent successful build is v0.9.38, predates several commits since. Cannot currently produce a fresh zip for the frozen commit. |

Rebuilding mobile+extension exactly at `d10b031` is possible on request —
flagging here rather than silently doing it, since mobile builds consume
EAS's monthly Android quota (already exhausted once this month, currently
using a workaround account) and each build takes real time.

## 4. CI/CD run IDs for the frozen commit

All at commit `d10b031f08d7e4e5a961c858b1024b07ed84ae60`, GitHub Actions on `main`:

| Workflow | Run ID | Result |
|---|---|---|
| `ci` | [33632080073](https://github.com/imasssad/Thanos-Wallet/actions/runs/33632080073) | Partial — see Known gaps #1/#2 for the 2 real failures; typecheck/lint/build(web)/tests all pass |
| `sbom` | [33632080440](https://github.com/imasssad/Thanos-Wallet/actions/runs/33632080440) | ✅ success |
| `deploy` | [33632080199](https://github.com/imasssad/Thanos-Wallet/actions/runs/33632080199) | Skipped (deploy is manual/gated, see §2) |

Full logs for the relevant jobs are saved locally in this package:
- `test-results/ci-tests-vault-auth-full.log`
- `vuln-scans/ci-dependency-audit-full.log`

## 5. Dependency lockfiles + SBOM

- Lockfiles: `pnpm-lock.yaml` (root — covers web/desktop/extension/packages/services) + `apps/mobile/package-lock.json` (mobile is intentionally outside the pnpm workspace, see `pnpm-workspace.yaml`'s own comment).
- SBOM (CycloneDX + SPDX, generated by the `sbom` CI workflow for this exact commit, downloaded from run 33632080440):
  - `sbom/sbom-cyclonedx.json` — 2,422 components (web/desktop/extension/packages/services)
  - `sbom/sbom-spdx.json` — same data, SPDX rendering
  - `sbom/sbom-mobile.json` — 943 components (mobile's separate dependency tree)

## 6. Vulnerability scan

`vuln-scans/ci-dependency-audit-full.log` — real `pnpm audit` output from
CI for this exact commit: **19 vulnerabilities (1 low, 10 moderate, 8
high)**, all traced to `postcss`/`sharp`/transitive deps of `next` and
`@sentry/nextjs` in `apps/web`. None are in first-party code. Full
per-package breakdown is in the log (advisory IDs, affected/patched
version ranges, dependency paths).

**Mobile** (`apps/mobile`, separate `npm audit` — completed after this
doc's first pass, see `vuln-scans/npm-audit-mobile.json`): **53
vulnerabilities — 1 critical, 19 high, 17 moderate, 16 low.**

⚠️ **The critical finding needs attention before this goes to
auditors:** `protobufjs <=7.6.2`, pulled in transitively via
`@cosmjs/stargate` (Cosmos Hub support). Multiple CVEs bundled under
this one advisory: arbitrary code execution, prototype-pollution-to-
code-generation, and several DoS vectors. A fix exists —
`@cosmjs/stargate@0.39.0` — but it's a semver-major bump, so it needs
a real upgrade + regression pass on the Cosmos client (`packages/sdk-core/src/clients/cosmos*`
and mobile's `lib/cosmos.ts`), not just a blind bump. Flagging rather
than bumping blind under audit-prep time pressure — this is exactly
the kind of change that deserves its own verification pass.

## 7. Architecture and security documentation

This repo already has substantial, code-grounded security documentation
that I verified against the actual source before including here (spot-
checked: `services/{api,indexer,worker}`, `ops/`, `ROLLBACK.md`, the 10
Playwright E2E specs, `apps/desktop/src/main/signer.ts`, and the
Argon2id/AES-256-GCM vault crypto all exist as claimed):

- **[docs/SECURITY-AUDIT-SCOPE.md](../../SECURITY-AUDIT-SCOPE.md)** — the
  RFQ scope doc: surfaces, threat-model tiers, what's in/out of scope.
  ⚠️ Its "Status" line still references a stale freeze commit (`3591004`,
  June 2026) — needs updating to `d10b031` / the new tag before sending
  to any audit firm. Not changed here since that's a judgment call on
  whether THIS is really the candidate to send out, not mine to make
  unilaterally.
- **[docs/SIGNING-ISOLATION.md](../../SIGNING-ISOLATION.md)** — per-client
  key-storage/isolation primitives (Web Worker / MV3 offscreen doc /
  Electron main-process IPC / RN module-private closure), with file:line
  citations and an explicit "what this does NOT protect against" section.
- **[docs/architecture.md](../../architecture.md)** — high-level surface
  map. Thinner than the above two; doesn't yet cover mnemonic/BIP39-44-84
  derivation, RPC handling, or JWT/session details in file:line depth —
  see Known gaps #4.
- **[docs/production-readiness-audit.md](../../production-readiness-audit.md)**
  — resilience/observability/deployment/testing scorecard, per-client
  multi-chain UI status, signing-isolation status table.

## 8. Existing automated test results

Pulled from CI (`ci-tests-vault-auth-full.log`), same commit, clean
Ubuntu runner (my own local Windows dev environment hit unrelated native-
binary instability today — CI is the trustworthy source here):

| Suite | Files | Tests | Result |
|---|---|---|---|
| `@thanos/sdk-core` | 14 | 140 | ✅ all pass — BIP39/44/84 derivation, address conversion (litho1↔0x), vault crypto round-trip, phishing classifier, ecosystem helpers |
| `@thanos/web` | 1 | 13 | ✅ all pass — vault encrypt/decrypt, wrong password, tampering |
| `@thanos/api` | 4 (+2 skipped) | 46 (+11 skipped) | ✅ all pass — auth routes (register/login/me/refresh/logout) against a mocked pg |
| `multx-adapter` | 4 | 26 | ✅ all pass — manifest integrity, fail-closed route/network/cap checks, independent-receipt-verification-before-RELEASED |

## 9. Negative / adversarial tests — ⚠️ not yet surveyed in depth

Not completed this pass — this needs a real, dedicated survey (grep for
existing wrong-chain/malformed/invalid/corrupted-signature coverage
across the test suites) plus honest gap identification, and possibly new
test authoring for anything missing. Flagging as open rather than
guessing at coverage. Sized right for a dedicated follow-up pass.

## 10. Environment / configuration documentation (secrets removed)

`.env.example` (repo root) is already the sanitized reference — every
var name present, no real values. Not duplicated here; linking instead
since duplicating risks drifting out of sync with the real file.

## 11. File manifest (SHA-256)

Not yet generated — do this last, once the package (including whichever
open items above get filled in) is actually final. Generating it now
against a partial package would just need regenerating.

## 12. Clean-environment build/test instructions

The `.github/workflows/ci.yml` workflow IS the tested, actually-working
clean-room procedure (it runs on a fresh Ubuntu runner on every push) —
pointing auditors at it directly is more trustworthy than a hand-written
doc that could drift from what CI actually does. Key commands, extracted:

```bash
git clone https://github.com/imasssad/Thanos-Wallet.git
cd Thanos-Wallet
git checkout audit-candidate-2026-08-27
corepack enable
pnpm install --frozen-lockfile   # NOT --no-frozen-lockfile — see Known gaps #1
pnpm typecheck
pnpm --filter @thanos/sdk-core test
pnpm --filter @thanos/web test
pnpm --filter @thanos/api test
pnpm --filter multx-adapter test
pnpm --filter @thanos/web build
# mobile (separate workspace):
cd apps/mobile && npm install && npm run typecheck
```

---

## Known gaps (things I found that are real, current, and worth knowing about)

1. **Extension build is currently broken**, both in CI and locally:
   `[vite-plugin-top-level-await] missing field 'type'`. Root cause: CI's
   `ci.yml` uses `pnpm install --no-frozen-lockfile`, which let `@swc/core`
   drift to `1.16.1` — incompatible with `vite-plugin-top-level-await@1.6.0`
   — even though the committed lockfile pins the last-known-good `1.15.33`.
   **This is a reproducibility problem, not just a broken build**: CI
   passing or failing right now depends on whatever the npm registry
   happens to resolve that day, not on what's actually locked. Fix is
   either pinning the offending package tighter or switching CI to
   `--frozen-lockfile` (the latter is more correct but touches every CI
   job, so I didn't do it unilaterally under time pressure — flagging for
   a decision).
2. **The Postgres integration test job fails** with `duplicate key value
   violates unique constraint "pg_extension_name_index"` in
   `contacts-integration.test.ts:56`. Looks like a non-idempotent
   extension-registration step (e.g. `CREATE EXTENSION` without `IF NOT
   EXISTS`) racing across parallel test files, not a real app bug — but
   unverified, flagging rather than assuming.
3. **Mobile has a critical-severity dependency vulnerability** —
   `protobufjs <=7.6.2` (via `@cosmjs/stargate`), covering arbitrary
   code execution and prototype-pollution CVEs. See §6 above for the
   fix path. This is the single highest-priority finding in this whole
   package and should be resolved (or at minimum triaged with a
   documented risk acceptance) before the audit starts, not left for
   the auditors to find.
4. `docs/architecture.md` doesn't yet have file:line-level coverage of
   mnemonic/seed generation, BIP39/44/84 derivation, EVM/Bitcoin/
   Lithosphere network handling specifics, RPC handling, or JWT/session
   implementation the way `SIGNING-ISOLATION.md` does for key storage —
   worth expanding to match that doc's rigor before this goes to auditors.
5. Items 3 (fresh mobile+extension artifacts exactly at this commit), 9
   (adversarial test survey), and 11 (final file-manifest hashes) are
   open — sized right for explicit follow-up rather than rushed here.
